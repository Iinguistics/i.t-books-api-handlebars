# i.t-books-api-handlebars

Simple book search to find tech books. This app uses Node, Express and Handlebars

# Install dependencies

npm install

# Serve on localhost:5000

npm start

# Dev Server (Nodemon)

npm run dev
